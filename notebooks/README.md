# Notebooks

Notebooks are for exploration, reports, and demonstrations. Production logic must be moved into `src/leaseguard` and tested before it is used by the main pipeline.

Do not commit notebook outputs containing lease text, personal information, or large generated files.
