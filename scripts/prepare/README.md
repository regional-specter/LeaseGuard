# Preparation Scripts

- `export_schemas.py` writes public Pydantic models to `data/schemas/`.
- `build_dataset.py` validates Dataset v1 rules, builds the seed or an examples directory, and prints statistics.
