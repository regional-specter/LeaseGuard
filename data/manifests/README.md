# Source Manifests

Source manifests will record where each document came from, its license, jurisdiction, document type, retrieval date, checksum, and related-document family.

`phase2_validation_sources.json` records the small office, retail-capable, municipal, and amendment sources used to validate ontology version `1.0.0`. Rights marked `review_required` must never be treated as approved training data.
