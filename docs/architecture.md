# Architecture

LeaseGuard will combine document processing, retrieval, deterministic validation, and a fine-tuned language model. This document will record component boundaries and the decisions that connect them.

Architecture decisions should favor traceable evidence, local operation, and replaceable components.
