# Product Scope

This document will define LeaseGuard's first jurisdiction, lease type, users, supported inputs, outputs, safety limits, and non-goals.

These decisions must be completed before the lease ontology and dataset are finalized.
