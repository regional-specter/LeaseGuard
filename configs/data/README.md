# Data Configuration

`pipeline.v1.json` freezes parser limits: allowed media types, maximum file size, near-duplicate threshold, and the Colab download environment variable. OCR remains disabled.

`dataset.v1.json` freezes Dataset v1 inclusion, exclusion, quality, and family-split rules. The Git seed under `data/samples/dataset_v1/` is the only local corpus.
